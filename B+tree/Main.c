/*#include <stdio.h>
#include <stdlib.h>

int add_leaf()

int main()
{
	enum argument{EXIT, ADD, DEL, SHOW};
	enum argument arg;

	do{
		printf("0 - exit\n1 - add new element\n2 - delete existing element\n3 - show the tree\n\n");
		scanf("%d", &arg);
		switch (arg)
		{
		case EXIT: break;
		case ADD: ; break;
		case DEL: ; break;
		case SHOW:; break;
		default: printf("\nError: Invalid command: %d\n\n", arg);
		}
	} while (arg != EXIT);

	return 0;
}*/